# 接口权限匹配

> **版本**：v1.1 · **更新**：2026-07-31 · **核对源码**：`backend/app/routers/*.py` 全部 17 个路由文件
> **接口总数**：100（原 92，补充通知 SSE/type-counts、通知消息字典模块、重置权限覆盖 8 个接口）

## 权限判断表达式说明

| 符号   | 含义                        | 优先级 |
| ------ | --------------------------- | ------ |
| `()`   | 分组，强制提升优先级        | 最高   |
| `!`    | 非（NOT），对后面的条件取反 | 高     |
| `&`    | 且（AND），必须全部满足     | 中     |
| `\|\|` | 或（OR），满足任一条件即可  | 低     |

> **优先级示例**：`A & B || C` 等价于 `(A & B) || C`；`!A & B` 等价于 `(!A) & B`。

## 内置判断函数

### 函数列表与参数

> 完整函数定义见 [权限表达式内置判断函数](权限表达式内置判断函数.md)。

| 函数              | 参数形式                          | 默认 ctx key  | 含义                                       |
| ----------------- | --------------------------------- | ------------- | ------------------------------------------ |
| `isAuthenticated` | 无参                              | —             | 当前用户已登录（JWT 校验通过）             |
| `isSuperAdmin`    | 无参                              | —             | 当前用户为超级管理员（role=admin，ID="1"） |
| `isAdmin`         | 无参                              | —             | 当前用户为管理员（role=admin 或 manager）  |
| `isSelf`          | 无参 / `isSelf(key_name)`         | `user_id`     | 目标用户为当前登录用户自身                 |
| `isBuiltInAdmin`  | 无参 / `isBuiltInAdmin(key_name)` | `user_id`     | 目标用户为内置超级管理员（ID="1"）         |
| `isOwnAccount`    | 无参 / `isOwnAccount(key_name)`   | `account`     | 账号的 user_id 等于当前用户 ID             |
| `isOwnContent`    | 无参 / `isOwnContent(key_name)`   | `content`     | 内容的 user_id 等于当前用户 ID             |
| `hasSameRole`     | 无参 / `hasSameRole(key_name)`    | `target_user` | 当前用户与目标用户存在共同角色             |
| `isAdminType`     | `isAdminType(role_type)`          | —             | 角色 role_type 为 "admin"                  |
| `isOtherType`     | `isOtherType(role_type)`          | —             | 角色 role_type 为 "other"                  |

### 参数说明

- **无参形式**：函数从上下文（ctx）中读取默认 key 对应的值进行判断。
- **有参形式**：参数为 ctx 字典中的 **key 名称**（字符串），函数从 `ctx[key_name]` 取值。
  - 例如 `isSelf(user_id)` → 读取 `ctx["user_id"]` 作为目标用户 ID
  - 例如 `isOwnAccount(acc)` → 读取 `ctx["acc"]` 作为账号对象（需具有 `user_id` 字段）
- **参数不是字面值**：`isSelf("1")` 是错误的用法，应使用 `isSelf(user_id)` 配合 `ctx = { user_id: "1" }`。

### 上下文（ctx）字段定义

#### 前端 PermContext（`frontend/src/utils/permExpression.ts`）

```typescript
export interface PermContext {
  currentUser: { id: string; role: string } // 必填，由 userStore 自动注入
  user_id?: string // isSelf / isBuiltInAdmin 默认读取
  account?: { user_id: string } // isOwnAccount 默认读取
  content?: { user_id: string } // isOwnContent 默认读取
  target_user?: { id: string; role: string } // hasSameRole 默认读取
  current_role_ids?: string[] // hasSameRole 当前用户角色集
  target_role_ids?: string[] // hasSameRole 目标用户角色集
}
```

#### 后端 context（`backend/app/services/perm_expression.py`）

```python
context: dict[str, Any] = {
    "current_user": User,              # 必填，由 require_permission 自动注入
    "user_id": str,                    # isSelf / isBuiltInAdmin 默认读取
    "account": Account,                # isOwnAccount 默认读取（需有 user_id 属性）
    "content": Content,                # isOwnContent 默认读取（需有 user_id 属性）
    "target_user": User,               # hasSameRole 默认读取
    "all_role_ids": set[str],          # hasSameRole 后备角色集
    "current_role_ids": set[str],      # hasSameRole 当前用户角色集
    "target_role_ids": set[str],       # hasSameRole 目标用户角色集
}
```

> **前后端差异**：前端 `currentUser` 为驼峰，后端 `current_user` 为下划线；前端角色集为 `string[]`，后端为 `set[str]`。

## 使用方式

### 1. 前端权限检查

#### 1.1 路由守卫（`router/index.ts`）

路由守卫自动将 `route.query` 和 `route.params` 合并为 ctx，传递给 `hasPermission`：

```typescript
function hasPerm(to: RouteLocationNormalized): boolean {
  if (to.meta.skipPermCheck) return true
  const permKey = to.meta.permKey as string | undefined
  if (!permKey) return true
  const permStore = usePermissionStore()
  return permStore.hasPermission(permKey, {
    ...(to.query ?? {}),
    ...(to.params ?? {}),
  })
}
```

路由配置示例：

```typescript
{
  path: '/rbac/users/:id/edit',
  component: RBACUserEdit,
  meta: {
    permKey: 'users:update:read || isSelf(user_id)',
    // ctx 中的 user_id 由路由 params.id 自动注入
  },
}
```

#### 1.2 组件内权限检查（`hasPermission`）

```typescript
import { usePermissionStore } from '@/stores/permission'

const permStore = usePermissionStore()

// 单一权限 key
const canCreate = computed(() => permStore.hasPermission('users:create:write'))

// 权限表达式（无内置函数）— 无需传 ctx
const canEdit = computed(() => permStore.hasPermission('users:update:write&isAdmin()'))

// 权限表达式（含 isSelf 等需上下文的函数）— 必须传 ctx
const canEditSelf = computed(() =>
  permStore.hasPermission('users:update:write||isSelf(user_id)', { user_id: userId }),
)

// 复合表达式
const canDelete = computed(() =>
  permStore.hasPermission('users:delete:write & !isBuiltInAdmin(user_id) & !isSelf(user_id)', {
    user_id: targetUserId,
  }),
)
```

#### 1.3 模板指令（`v-perm`）

```vue
<!-- 单一权限 key -->
<a-button v-perm="'users:create:write'">创建用户</a-button>

<!-- 权限表达式 + 上下文 -->
<a-button
  v-perm="{
    key: 'users:change_password:write || isSelf(user_id)',
    ctx: { user_id: record.id },
  }"
>
  修改密码
</a-button>
```

#### 1.4 权限存储（`stores/permission.ts`）

```typescript
function hasPermission(
  keyOrExpr: string, // 权限 key 或表达式
  ctx?: PermContext, // 上下文（可选，表达式含内置函数时必传）
): boolean {
  if (!isExpression(keyOrExpr)) {
    return keyOrExpr in permissions.value // 单一 key 直接查表
  }
  return evaluatePermission(
    keyOrExpr,
    permissions.value,
    _mergeContext(ctx), // 合并默认 ctx（currentUser）
  )
}
```

### 2. 后端权限校验

#### 2.1 装饰器模式（`@RequiresPermissions`）

```python
from app.core.deps import PermAPIRoute, RequiresPermissions

router = APIRouter(prefix="/api/v2", route_class=PermAPIRoute)

# 单一权限 key
@router.get("/users")
@RequiresPermissions("users:read")
async def list_users(...): ...

# 权限表达式（OR 逻辑）
@router.get("/permissions")
@RequiresPermissions("permissions:read||permissions:manage:read")
async def list_permissions(...): ...

# 权限表达式（含内置函数，需传 context）
@router.put("/users/{user_id}/password")
@RequiresPermissions(
    "users:change_password:write||isSelf(user_id)",
    context={"user_id": "path.user_id"},  # 从路径参数提取 user_id
)
async def change_password(...): ...
```

> **注意**：当前后端实际代码中，`@RequiresPermissions` 装饰器主要使用单一权限 key，权限表达式 DSL 主要在前端使用。后端 `require_permission` 已完整支持表达式求值，可通过 `context` 参数传递上下文。

#### 2.2 依赖工厂（`require_permission`）

```python
from app.core.deps import require_permission

# 单一权限 key
@router.get("/users", dependencies=[Depends(require_permission("users:read"))])
async def list_users(...): ...

# 权限表达式
@router.get(
    "/users/{user_id}",
    dependencies=[Depends(
        require_permission(
            "users:read&(isAdmin()||isSelf(user_id))",
            context={"user_id": "path.user_id"},
        )
    )],
)
async def get_user(...): ...
```

#### 2.3 上下文参数提取规则

`context` 参数用于声明如何从请求中提取内置函数所需的上下文值：

| 格式              | 说明           | 示例                                                |
| ----------------- | -------------- | --------------------------------------------------- |
| `"path.{param}"`  | 从路径参数提取 | `"path.user_id"` → `request.path_params["user_id"]` |
| `"query.{param}"` | 从查询参数提取 | `"query.filter"` → `request.query_params["filter"]` |
| `"body.{field}"`  | 从请求体提取   | `"body.target_id"` → `request_body["target_id"]`    |

> **当前实现**：`require_permission` 通过 `context` 参数接收静态字典，由调用方负责构造。路径参数的自动提取需在路由函数内手动传递。

### 3. 表达式求值机制

#### 3.1 前端求值流程

```
hasPermission(keyOrExpr, ctx?)
  ├─ 非表达式 → 查 permissions 字典
  └─ 是表达式 → evaluatePermission(expr, permissions, mergeContext(ctx))
                  ├─ Tokenizer 分词（PERM / FUNC / OR / AND / NOT / LPAREN / RPAREN）
                  ├─ Parser 递归下降解析（expr → andExpr → atom）
                  └─ atom 求值：
                       ├─ PERM → key in permissions
                       ├─ FUNC → BUILTIN_FUNCTIONS[name](args, ctx)
                       ├─ NOT → !atom
                       └─ LPAREN → (expr)
```

#### 3.2 后端求值流程

```
require_permission(key, context?)
  ├─ 非表达式 → 查 flatten_effective_permissions 结果
  └─ 是表达式 → evaluate_permission(expr, flat_perms, eval_ctx)
                  ├─ Tokenizer 分词
                  ├─ Parser 递归下降解析
                  └─ atom 求值：
                       ├─ PERM → key in flat_perms
                       ├─ FUNC → BUILTIN_FUNCTIONS[name](args, ctx, perm_keys, all_role_ids)
                       ├─ NOT → !atom
                       └─ LPAREN → (expr)
```

### 4. 表达式编写规范

1. **运算符必须成对**：`||` 是合法的，单个 `|` 会抛出 `SyntaxError`。
2. **权限 key 与函数的区分**：
   - 含 `:` 的标识符 → 权限 key（如 `users:read`）
   - 后接 `(` 的标识符 → 函数调用（如 `isSelf()`）
   - 在内置函数名集合中的标识符 → 函数（如 `isSelf`、`isAdmin`）
   - 其他标识符 → 按权限 key 处理
3. **函数参数为 ctx key**：参数必须是上下文字典中存在的 key 名称，不能是字面值。
4. **无参函数可省略括号**：`isAdmin` 与 `isAdmin()` 等价；`isSelf` 与 `isSelf()` 等价（使用默认 `ctx.user_id`）。
5. **空格可选**：`A& B` 与 `A & B` 等价，但建议保持一致风格。

---

## 一、认证模块 `/api/auth`

### 1. 用户登录

- **方法**: POST
- **路径**: `/api/auth/login`
- **描述**: 用户登录，返回 access_token 和用户信息
- **请求体** (`LoginRequest`):
  - `username` (str, 必填): 用户名
  - `password` (str, 必填): 密码
- **响应体** (`LoginResponse`):
  - `access_token` (str): JWT 令牌
  - `token_type` (str): 令牌类型，固定为 "bearer"
  - `user` (`UserInfo`): 用户信息
    - `id` (str): 用户 ID
    - `username` (str): 用户名
    - `email` (Optional[str]): 邮箱
    - `nickname` (str): 昵称
    - `role` (str): 角色标识
    - `avatar_url` (Optional[str]): 头像 URL
    - `created_at` (datetime): 创建时间
- **权限表达式**: 公开，无需权限

### 2. 用户注册

- **方法**: POST
- **路径**: `/api/auth/register`
- **描述**: 用户注册（admin 角色不可通过注册创建）
- **请求体** (`UserCreate`):
  - `username` (str, 必填): 用户名
  - `password` (str, 必填): 密码
  - `nickname` (str, 必填): 昵称
  - `email` (Optional[str], 选填): 邮箱
  - `role` (str, 选填, 默认 "operator"): 角色标识（不可为 admin）
  - `avatar_url` (Optional[str], 选填): 头像 URL
- **响应体** (`UserInfo`): 结构同登录返回的 `user`
- **权限表达式**: 公开，无需权限

### 3. 获取当前用户信息

- **方法**: GET
- **路径**: `/api/auth/me`
- **描述**: 获取当前登录用户信息（含角色和权限枚举）
- **响应体** (`UserInfoWithRoles`): 继承 `UserInfo`，扩展：
  - `roles` (list[`UserRoleRef`]): 角色列表
    - `id` (str): 角色 ID
    - `name` (str): 角色标识
    - `display_name` (str): 角色显示名
  - `permissions` (dict[str, str]): 项目全部权限枚举（key → 资源名）
- **权限表达式**: `isAuthenticated`

### 4. 更新个人信息

- **方法**: PUT
- **路径**: `/api/auth/profile`
- **描述**: 更新当前用户个人信息（昵称、头像）
- **请求体** (`ProfileUpdate`):
  - `nickname` (Optional[str], 选填): 昵称
  - `avatar_url` (Optional[str], 选填): 头像 URL
- **响应体** (`UserInfo`): 更新后的用户信息
- **权限表达式**: `isAuthenticated`

### 5. 修改当前用户密码

- **方法**: PUT
- **路径**: `/api/auth/password`
- **描述**: 修改当前用户密码（需验证旧密码）
- **请求体** (`PasswordChange`):
  - `old_password` (str, 必填): 旧密码
  - `new_password` (str, 必填): 新密码
- **响应体**:
  - `message` (str): 提示信息
- **权限表达式**: `isAuthenticated`

---

## 二、仪表盘模块 `/api/dashboard`

### 1. 获取仪表盘统计数据

- **方法**: GET
- **路径**: `/api/dashboard/stats`
- **描述**: 获取当前用户的仪表盘统计数据，包括账号总数、今日发布数、待处理任务数、AI 生成内容数、各平台统计及最近发布列表
- **响应体** (`DashboardStats`):
  - `total_accounts` (int): 总账号数
  - `today_published` (int): 今日发布数
  - `pending_tasks` (int): 待处理任务数
  - `ai_generated_count` (int): AI 生成内容数
  - `platform_stats` (list[`PlatformStat`]): 各平台统计
    - `platform` (str): 平台 key
    - `name` (str): 平台中文名
    - `accounts` (int): 账号数
    - `active` (int): 活跃账号数
    - `articles` (int): 文章数
  - `recent_publishes` (list[`RecentPublish`]): 最近发布（5 条）
    - `id` (str): 任务 ID
    - `title` (str): 内容标题
    - `platform` (str): 平台
    - `account` (str): 账号昵称
    - `status` (str): 任务状态
    - `time` (str): 创建时间（HH:MM）
- **权限表达式**: `dashboard:read`

---

## 三、平台账号模块 `/api/accounts`

### 1. 获取账号列表

- **方法**: GET
- **路径**: `/api/accounts/`
- **描述**: 获取当前用户的账号列表，可按平台过滤
- **查询参数**:
  - `platform` (str, 选填): 平台过滤
- **响应体**: list[`AccountResponse`]
  - `id` (str): 账号 ID
  - `user_id` (str): 用户 ID
  - `platform` (str): 平台
  - `nickname` (str): 昵称
  - `avatar_url` (Optional[str]): 头像 URL
  - `status` (str): 状态
  - `cookie_data` (Optional[str]): Cookie 数据
  - `access_token` (Optional[str]): 访问令牌
  - `token_expires_at` (Optional[datetime]): 令牌过期时间
  - `last_check_at` (Optional[datetime]): 最后检查时间
  - `error_message` (Optional[str]): 错误信息
  - `created_at` (datetime): 创建时间
  - `updated_at` (datetime): 更新时间
- **权限表达式**: `accounts:read`

### 2. 创建账号

- **方法**: POST
- **路径**: `/api/accounts/`
- **描述**: 为当前用户创建一个新账号
- **请求体** (`AccountCreate`):
  - `platform` (str, 必填): 平台
  - `nickname` (str, 必填): 昵称
  - `avatar_url` (Optional[str], 选填): 头像 URL
  - `cookie_data` (Optional[str], 选填): Cookie 数据
  - `access_token` (Optional[str], 选填): 访问令牌
  - `token_expires_at` (Optional[datetime], 选填): 令牌过期时间
- **响应体** (`AccountResponse`): 结构同列表项
- **权限表达式**: `account:create:write`

### 3. 获取账号详情

- **方法**: GET
- **路径**: `/api/accounts/{account_id}`
- **描述**: 获取指定账号详情
- **路径参数**:
  - `account_id` (str): 账号 ID
- **响应体** (`AccountResponse`)
- **权限表达式**: `accounts:read&isOwnAccount(account)`

### 4. 更新账号

- **方法**: PUT
- **路径**: `/api/accounts/{account_id}`
- **描述**: 更新指定账号信息
- **路径参数**:
  - `account_id` (str): 账号 ID
- **请求体** (`AccountUpdate`, 均选填):
  - `nickname` (Optional[str])
  - `avatar_url` (Optional[str])
  - `status` (Optional[str])
  - `cookie_data` (Optional[str])
  - `access_token` (Optional[str])
  - `token_expires_at` (Optional[datetime])
- **响应体** (`AccountResponse`)
- **权限表达式**: `account:update:write&isOwnAccount(account)`

### 5. 删除账号

- **方法**: DELETE
- **路径**: `/api/accounts/{account_id}`
- **描述**: 删除指定账号
- **路径参数**:
  - `account_id` (str): 账号 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `account:delete:write&isOwnAccount(account)`

### 6. 检查账号状态

- **方法**: POST
- **路径**: `/api/accounts/{account_id}/check`
- **描述**: 通过平台适配器检查账号的实时状态
- **路径参数**:
  - `account_id` (str): 账号 ID
- **响应体** (`AccountStatusResponse`):
  - `id` (str): 账号 ID
  - `status` (str): 状态
  - `last_check_at` (Optional[datetime]): 最后检查时间
  - `error_message` (Optional[str]): 错误信息
- **权限表达式**: `account:check:write&isOwnAccount(account)`

---

## 四、内容管理模块 `/api/contents`

### 1. 获取内容列表

- **方法**: GET
- **路径**: `/api/contents/`
- **描述**: 分页获取当前用户的内容列表，可按平台和状态过滤
- **查询参数**:
  - `platform` (str, 选填): 平台过滤
  - `status` (str, 选填, 别名 `status_filter`): 状态过滤
  - `page` (int, 默认 1, ≥1): 页码
  - `page_size` (int, 默认 20, 1~100): 每页条数
- **响应体**: list[`ContentResponse`]
  - `id` (str): 内容 ID
  - `user_id` (str): 用户 ID
  - `title` (str): 标题
  - `body` (str): 正文
  - `platform` (str): 平台
  - `status` (str): 状态
  - `media_urls` (Optional[str]): 媒体 URL
  - `ai_generated` (bool): 是否 AI 生成
  - `original_content_id` (Optional[str]): 原始内容 ID
  - `created_at` (datetime): 创建时间
  - `updated_at` (datetime): 更新时间
- **权限表达式**: `content:read`

### 2. 创建内容

- **方法**: POST
- **路径**: `/api/contents/`
- **描述**: 创建一条新内容
- **请求体** (`ContentCreate`):
  - `title` (str, 必填): 标题
  - `body` (str, 必填): 正文
  - `platform` (str, 必填): 平台
  - `status` (str, 选填, 默认 "draft"): 状态
  - `media_urls` (Optional[str], 选填): 媒体 URL
- **响应体** (`ContentResponse`)
- **权限表达式**: `content:create:write`

### 3. 获取 AI 生成记录列表

- **方法**: GET
- **路径**: `/api/contents/ai-generations`
- **描述**: 分页获取当前用户的 AI 生成记录
- **查询参数**:
  - `platform` (str, 选填)
  - `page` (int, 默认 1, ≥1)
  - `page_size` (int, 默认 20, 1~100)
- **响应体**: list[`AIGenerationRecordResponse`]
  - `id` (str): 记录 ID
  - `user_id` (str): 用户 ID
  - `topic` (str): 主题
  - `platform` (str): 平台
  - `plan_id` (Optional[str]): 方案 ID
  - `model` (Optional[str]): 模型名
  - `title` (str): 标题
  - `body` (str): 正文
  - `hashtags` (list[str]): 标签列表
  - `created_at` (datetime): 创建时间
- **权限表达式**: `content:read`

### 4. 获取内容详情

- **方法**: GET
- **路径**: `/api/contents/{content_id}`
- **描述**: 获取指定内容详情
- **路径参数**:
  - `content_id` (str): 内容 ID
- **响应体** (`ContentResponse`)
- **权限表达式**: `content:read&isOwnContent(content)`

### 5. 更新内容

- **方法**: PUT
- **路径**: `/api/contents/{content_id}`
- **描述**: 更新指定内容
- **路径参数**:
  - `content_id` (str): 内容 ID
- **请求体** (`ContentUpdate`, 均选填):
  - `title` (Optional[str])
  - `body` (Optional[str])
  - `platform` (Optional[str])
  - `status` (Optional[str])
  - `media_urls` (Optional[str])
- **响应体** (`ContentResponse`)
- **权限表达式**: `content:update:write&isOwnContent(content)`

### 6. 删除内容

- **方法**: DELETE
- **路径**: `/api/contents/{content_id}`
- **描述**: 删除指定内容
- **路径参数**:
  - `content_id` (str): 内容 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `content:delete:write&isOwnContent(content)`

### 7. AI 生成内容（非流式）

- **方法**: POST
- **路径**: `/api/contents/ai-generate`
- **描述**: 调用 LLM 生成内容变体，并写入 AI 生成记录
- **请求体** (`AIGenerateRequest`):
  - `topic` (str, 必填): 主题
  - `platform` (str, 必填): 平台
  - `style` (Optional[str], 选填): 风格
  - `keywords` (Optional[list[str]], 选填): 关键词
  - `count` (int, 选填, 默认 3): 生成数量
  - `plan_id` (Optional[str], 选填): 方案 ID
- **响应体** (`AIGenerateResponse`):
  - `variants` (list[`AIVariant`]): 生成结果
    - `title` (str): 标题
    - `body` (str): 正文
    - `hashtags` (list[str]): 标签
- **权限表达式**: `content:ai_generate:write`

### 8. AI 生成内容（SSE 流式）

- **方法**: POST
- **路径**: `/api/contents/ai-generate-stream`
- **描述**: SSE 流式生成端点，逐平台流式输出 LLM 内容，同时推送实时日志
- **请求体** (`AIGenerateStreamRequest`):
  - `topic` (str, 必填): 主题
  - `platforms` (list[str], 必填): 平台列表
  - `style` (Optional[str], 选填): 风格
  - `keywords` (Optional[list[str]], 选填): 关键词
  - `plan_id` (Optional[str], 选填): 方案 ID
  - `model_id` (Optional[str], 选填): 模型 ID
  - `files` (Optional[list[`UploadedFile`]], 选填): 上传文件
    - `data` (str): 文件数据
    - `mime_type` (str): MIME 类型
- **响应体**: `StreamingResponse`（`text/event-stream`），SSE 事件：
  - `log`: 实时日志
  - `chunk`: 流式文本块
  - `done`: 单平台生成完成（含 variant）
  - `error`: 错误信息
  - `complete`: 全部生成完成
- **权限表达式**: `content:ai_generate:write`

---

## 五、发布管理模块 `/api/publish`

### 1. 获取发布任务列表

- **方法**: GET
- **路径**: `/api/publish/tasks`
- **描述**: 分页获取当前用户的发布任务列表，可按状态过滤
- **查询参数**:
  - `status` (str, 选填): 状态过滤
  - `page` (int, 默认 1, ≥1)
  - `page_size` (int, 默认 20, 1~100)
- **响应体**: list[`PublishTaskResponse`]
  - `id` (str): 任务 ID
  - `content_id` (str): 内容 ID
  - `account_id` (str): 账号 ID
  - `status` (str): 状态
  - `scheduled_at` (Optional[datetime]): 计划发布时间
  - `published_at` (Optional[datetime]): 实际发布时间
  - `error_message` (Optional[str]): 错误信息
  - `retry_count` (int): 重试次数
  - `created_at` (datetime): 创建时间
  - `updated_at` (datetime): 更新时间
- **权限表达式**: `publish:read`

### 2. 创建发布任务

- **方法**: POST
- **路径**: `/api/publish/tasks`
- **描述**: 创建发布任务
- **请求体** (`PublishTaskCreate`):
  - `content_id` (str, 必填): 内容 ID
  - `account_id` (str, 必填): 账号 ID
  - `scheduled_at` (Optional[datetime], 选填): 计划发布时间
- **响应体** (`PublishTaskResponse`)
- **权限表达式**: `publish:create:write`

### 3. 获取发布任务详情

- **方法**: GET
- **路径**: `/api/publish/tasks/{task_id}`
- **描述**: 获取指定发布任务详情
- **路径参数**:
  - `task_id` (str): 任务 ID
- **响应体** (`PublishTaskResponse`)
- **权限表达式**: `publish:read`

### 4. 重试发布任务

- **方法**: POST
- **路径**: `/api/publish/tasks/{task_id}/retry`
- **描述**: 重试指定的发布任务
- **路径参数**:
  - `task_id` (str): 任务 ID
- **响应体** (`PublishTaskResponse`)
- **权限表达式**: `publish:retry:write`

### 5. 获取发布统计

- **方法**: GET
- **路径**: `/api/publish/stats`
- **描述**: 获取发布任务状态统计
- **响应体** (`PublishStatusResponse`):
  - `total` (int): 总数
  - `pending` (int): 待发布
  - `publishing` (int): 发布中
  - `published` (int): 已发布
  - `failed` (int): 失败
- **权限表达式**: `publish:read`

---

## 六、模板管理模块 `/api/templates`

### 1. 获取模板列表

- **方法**: GET
- **路径**: `/api/templates/`
- **描述**: 获取模板列表，可按平台过滤
- **查询参数**:
  - `platform` (str, 选填): 平台过滤
- **响应体**: list[`TemplateResponse`]
  - `id` (str): 模板 ID
  - `name` (str): 名称
  - `platform` (str): 平台
  - `thumbnail_url` (Optional[str]): 缩略图 URL
  - `config` (Optional[str]): 配置
  - `created_at` (datetime): 创建时间
  - `updated_at` (datetime): 更新时间
- **权限表达式**: `templates:read`

### 2. 创建模板

- **方法**: POST
- **路径**: `/api/templates/`
- **描述**: 创建一个新模板
- **请求体** (`TemplateCreate`):
  - `name` (str, 必填): 名称
  - `platform` (str, 必填): 平台
  - `thumbnail_url` (Optional[str], 选填): 缩略图 URL
  - `config` (Optional[str], 选填): 配置
- **响应体** (`TemplateResponse`)
- **权限表达式**: `templates:create:write`

### 3. 获取模板详情

- **方法**: GET
- **路径**: `/api/templates/{template_id}`
- **描述**: 获取指定模板详情
- **路径参数**:
  - `template_id` (str): 模板 ID
- **响应体** (`TemplateResponse`)
- **权限表达式**: `templates:read`

### 4. 更新模板

- **方法**: PUT
- **路径**: `/api/templates/{template_id}`
- **描述**: 更新指定模板
- **路径参数**:
  - `template_id` (str): 模板 ID
- **请求体** (`TemplateUpdate`, 均选填):
  - `name` (Optional[str])
  - `platform` (Optional[str])
  - `thumbnail_url` (Optional[str])
  - `config` (Optional[str])
- **响应体** (`TemplateResponse`)
- **权限表达式**: `templates:update:write`

### 5. 删除模板

- **方法**: DELETE
- **路径**: `/api/templates/{template_id}`
- **描述**: 删除指定模板
- **路径参数**:
  - `template_id` (str): 模板 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `templates:delete:write`

---

## 七、审核管理模块 `/api/reviews`

### 1. 获取待审核列表

- **方法**: GET
- **路径**: `/api/reviews/`
- **描述**: 获取所有状态为 `pending_review` 的内容列表
- **响应体**: list[`ReviewResponse`]
  - `id` (str): 内容 ID
  - `title` (str): 标题
  - `body` (str): 正文
  - `platform` (str): 平台
  - `status` (str): 状态
  - `created_at` (datetime): 创建时间
  - `user_id` (str): 提交者 ID
  - `username` (Optional[str]): 提交者用户名
- **权限表达式**: `review:read`

### 2. 提交内容审核

- **方法**: POST
- **路径**: `/api/reviews/{content_id}/submit`
- **描述**: 将指定内容状态改为 `pending_review`，并向拥有 `review:approve` 权限的用户发送通知
- **路径参数**:
  - `content_id` (str): 内容 ID
- **响应体** (`ReviewResponse`)
- **权限表达式**: `review:submit:write`

### 3. 审核通过

- **方法**: POST
- **路径**: `/api/reviews/{content_id}/approve`
- **描述**: 将内容状态改为 `ready`，并向内容创建者发送通知
- **路径参数**:
  - `content_id` (str): 内容 ID
- **响应体** (`ReviewResponse`)
- **权限表达式**: `review:approve:write`

### 4. 审核驳回

- **方法**: POST
- **路径**: `/api/reviews/{content_id}/reject`
- **描述**: 将内容状态改为 `rejected`，并向内容创建者发送通知（含驳回原因）
- **路径参数**:
  - `content_id` (str): 内容 ID
- **请求体**:
  - `reason` (str, 必填): 驳回原因
- **响应体** (`ReviewResponse`)
- **权限表达式**: `review:reject:write`

---

## 八、用户注册审核模块 `/api/user-creation-reviews`

### 1. 获取待审核用户创建请求列表

- **方法**: GET
- **路径**: `/api/user-creation-reviews/`
- **描述**: 获取状态为 `pending` 的用户创建请求列表
- **响应体**: list[`UserCreationRequestResponse`]
  - `id` (str): 请求 ID
  - `requester_id` (str): 申请人 ID
  - `requester_name` (str): 申请人用户名
  - `username` (str): 待创建用户名
  - `email` (Optional[str]): 邮箱
  - `nickname` (str): 昵称
  - `role` (str): 角色
  - `status` (str): 状态
  - `reviewer_id` (Optional[str]): 审核人 ID
  - `reviewer_name` (Optional[str]): 审核人用户名
  - `reject_reason` (Optional[str]): 驳回原因
  - `created_at` (datetime): 创建时间
- **权限表达式**: `review:read`

### 2. 通过用户创建请求

- **方法**: POST
- **路径**: `/api/user-creation-reviews/{request_id}/approve`
- **描述**: 审核通过后创建实际用户记录
- **路径参数**:
  - `request_id` (str): 请求 ID
- **响应体** (`UserCreationRequestResponse`)
- **权限表达式**: `review:approve:write`

### 3. 驳回用户创建请求

- **方法**: POST
- **路径**: `/api/user-creation-reviews/{request_id}/reject`
- **描述**: 驳回用户创建请求，记录驳回原因
- **路径参数**:
  - `request_id` (str): 请求 ID
- **请求体** (`RejectRequest`):
  - `reason` (str, 必填): 驳回原因
- **响应体** (`UserCreationRequestResponse`)
- **权限表达式**: `review:reject:write`

---

## 九、模型配置模块 `/api/model-configs`

### 1. 获取模型配置列表

- **方法**: GET
- **路径**: `/api/model-configs`
- **描述**: 获取所有模型配置列表
- **响应体** (`ModelConfigListResponse`):
  - `data` (list[`ModelConfigResponse`])
    - `id` (str): 配置 ID
    - `name` (str): 名称
    - `display_name` (str): 显示名
    - `provider` (str): 供应商
    - `mode` (str): 模式
    - `api_format` (str): API 格式
    - `api_key` (Optional[str]): API 密钥
    - `base_url` (Optional[str]): 基础 URL
    - `full_url` (bool): 是否为完整 URL
    - `model` (str): 模型名
    - `multimodal` (bool): 是否多模态
    - `model_series` (str): 模型系列
    - `context_input` (int): 输入上下文
    - `context_output` (int): 输出上下文
    - `tool_call_rounds` (int): 工具调用轮次
    - `enabled` (bool): 是否启用
    - `monthly_quota` (int): 月配额
    - `used_tokens` (int): 已用 tokens
    - `created_at` (Optional[datetime]): 创建时间
    - `updated_at` (Optional[datetime]): 更新时间
- **权限表达式**: `token_plan:read`

### 2. 创建模型配置

- **方法**: POST
- **路径**: `/api/model-configs`
- **描述**: 创建一个模型配置
- **请求体** (`ModelConfigCreate`):
  - `id` (Optional[str], 选填, 最长 64): 配置 ID
  - `name` (str, 必填, 最长 100): 名称
  - `display_name` (str, 必填, 最长 100): 显示名
  - `provider` (str, 必填, 最长 20): 供应商
  - `mode` (str, 必填, 最长 20): 模式
  - `api_format` (str, 选填, 默认 "openai_chat", 最长 50): API 格式
  - `api_key` (Optional[str], 选填, 最长 500): API 密钥
  - `base_url` (Optional[str], 选填, 最长 500): 基础 URL
  - `full_url` (bool, 选填, 默认 false): 是否完整 URL
  - `model` (str, 必填, 最长 2000): 模型名
  - `multimodal` (bool, 选填, 默认 false): 是否多模态
  - `model_series` (str, 选填, 默认 "default", 最长 50): 模型系列
  - `context_input` (int, 选填, 默认 128000): 输入上下文
  - `context_output` (int, 选填, 默认 4096): 输出上下文
  - `tool_call_rounds` (int, 选填, 默认 200): 工具调用轮次
  - `enabled` (bool, 选填, 默认 false): 是否启用
  - `monthly_quota` (int, 选填, 默认 1000000): 月配额
  - `used_tokens` (int, 选填, 默认 0): 已用 tokens
- **响应体** (`ModelConfigResponse`)
- **权限表达式**: `model_config:create:write`

### 3. 更新模型配置

- **方法**: PUT
- **路径**: `/api/model-configs/{config_id}`
- **描述**: 更新指定模型配置
- **路径参数**:
  - `config_id` (str): 配置 ID
- **请求体** (`ModelConfigUpdate`, 均选填): 字段同 `ModelConfigCreate`
- **响应体** (`ModelConfigResponse`)
- **权限表达式**: `model_config:update:write`

### 4. 删除模型配置

- **方法**: DELETE
- **路径**: `/api/model-configs/{config_id}`
- **描述**: 删除指定模型配置
- **路径参数**:
  - `config_id` (str): 配置 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `model_config:delete:write`

---

## 十、AI 模型模块 `/api/models`

### 1. 拉取模型列表

- **方法**: POST
- **路径**: `/api/models/fetch`
- **描述**: 通过指定 `base_url` 和 `api_key` 调用服务商 `/models` 接口拉取可用模型列表
- **请求体** (`FetchModelsRequest`):
  - `base_url` (str, 必填): 基础 URL
  - `api_key` (str, 必填): API 密钥
- **响应体** (`FetchModelsResponse`):
  - `models` (list[str]): 排序后的模型 ID 列表
- **权限表达式**: `token_plan:read`

---

## 十一、通知模块 `/api/notifications`

> 该模块未挂载 `PermAPIRoute`，所有接口仅校验 JWT 登录态（`isAuthenticated`），无 `@RequiresPermissions` 装饰器。

### 1. 通知 SSE 实时推送

- **方法**: GET
- **路径**: `/api/notifications/stream`
- **描述**: Server-Sent Events 长连接，实时推送当前用户的新通知（含 DB 轮询 + 内存广播双通道）
- **查询参数**:
  - `token` (str, 必填): JWT 令牌（因 SSE 无法自定义 Header，通过 query 传递）
- **响应体**: `StreamingResponse`（`text/event-stream`），SSE 事件：
  - `connected`: 连接建立（`{"status":"ok"}`）
  - `notification`: 新通知（`data` 为 `NotificationResponse` 数组）
- **权限表达式**: `isAuthenticated`（通过 query token 校验）
- **说明**: 响应头含 `Cache-Control: no-cache`、`Connection: keep-alive`、`X-Accel-Buffering: no`

### 2. 获取通知列表

- **方法**: GET
- **路径**: `/api/notifications/`
- **描述**: 获取当前用户的通知列表（分页）
- **查询参数**:
  - `page` (int, 默认 1): 页码
  - `page_size` (int, 默认 20): 每页条数
  - `type` (str, 选填): 通知类型过滤
- **响应体** (`NotificationListResponse`):
  - `total` (int): 总数
  - `page` (int): 当前页
  - `page_size` (int): 每页条数
  - `items` (list[`NotificationResponse`])
    - `id` (str): 通知 ID
    - `type` (str): 类型
    - `title` (str): 标题
    - `content` (Optional[str]): 内容
    - `related_id` (Optional[str]): 关联 ID
    - `is_read` (bool): 是否已读
    - `created_at` (datetime): 创建时间
- **权限表达式**: `isAuthenticated`

### 3. 获取未读通知数量

- **方法**: GET
- **路径**: `/api/notifications/unread-count`
- **描述**: 获取当前用户的未读通知数量
- **响应体**:
  - `count` (int): 未读通知数
- **权限表达式**: `isAuthenticated`

### 4. 获取各类型通知数量

- **方法**: GET
- **路径**: `/api/notifications/type-counts`
- **描述**: 获取当前用户各通知类型的总数统计
- **响应体**:
  - `total` (int): 通知总数
  - `counts` (dict[str, int]): 各类型 → 数量映射
- **权限表达式**: `isAuthenticated`

### 5. 标记通知为已读

- **方法**: POST
- **路径**: `/api/notifications/{notification_id}/read`
- **描述**: 将指定通知标记为已读
- **路径参数**:
  - `notification_id` (str): 通知 ID
- **响应体**:
  - `message` (str): 提示信息
- **权限表达式**: `isAuthenticated`

### 6. 标记所有通知为已读

- **方法**: POST
- **路径**: `/api/notifications/read-all`
- **描述**: 将当前用户的所有未读通知标记为已读
- **响应体**:
  - `message` (str): 提示信息
- **权限表达式**: `isAuthenticated`

---

## 十二、数据库模块 `/api/db`

### 1. 查询 SQL 执行历史

- **方法**: GET
- **路径**: `/api/db/history`
- **描述**: 分页获取 SQL 执行历史记录
- **查询参数**:
  - `page` (int, 默认 1, ≥1): 页码
  - `page_size` (int, 默认 20, 1~200): 每页条数
- **响应体** (`SqlHistoryResponse`):
  - `items` (list[`SqlHistoryItem`])
    - `id` (str): 记录 ID
    - `username` (str): 用户名
    - `sql_text` (str): SQL 语句
    - `is_success` (bool): 是否成功
    - `message` (str): 执行信息
    - `created_at` (str): 创建时间（ISO 字符串）
  - `total` (int): 总数
  - `page` (int): 当前页
  - `page_size` (int): 每页条数
- **权限表达式**: `db_history:view:read`

### 2. 执行 SQL

- **方法**: POST
- **路径**: `/api/db/execute`
- **描述**: 执行 SQL（查询/DDL/DML）。DELETE/UPDATE 不会直接执行，会返回 `need_review=True`；DROP/TRUNCATE/ALTER 等危险命令被禁止。SELECT 自动分页
- **请求体** (`SqlRequest`):
  - `sql` (str, 必填): SQL 命令
  - `page` (int, 选填, 默认 1): 页码
  - `page_size` (int, 选填, 默认 20): 每页条数
- **响应体** (`SqlResponse`):
  - `success` (bool): 是否成功
  - `message` (str): 执行信息
  - `columns` (list[str]): 列名（查询时）
  - `rows` (list[list]): 行数据（查询时）
  - `row_count` (int): 行数
  - `total_count` (int): 总数
  - `page_size` (int): 每页条数
  - `is_query` (bool): 是否为查询
  - `need_review` (bool): 是否需要审核
  - `change_type` (Optional[str]): 变更类型（delete/update）
- **权限表达式**: `db:execute:write`

---

## 十三、SQL 变更审核模块 `/api/db-changes`

### 1. 提交 SQL 变更审核

- **方法**: POST
- **路径**: `/api/db-changes/submit`
- **描述**: 提交 DELETE/UPDATE 类 SQL 变更审核请求，并向拥有 `db_change:approve` 权限的用户发送通知。需 2 人审核通过后才会自动执行
- **请求体** (`SubmitChangeRequest`):
  - `sql` (str, 必填): SQL 命令
  - `change_type` (str, 必填): 变更类型（update / delete / row_delete）
  - `description` (Optional[str], 选填): 描述
- **响应体** (`SqlChangeItem`):
  - `id` (str): 请求 ID
  - `requester_id` (str): 申请人 ID
  - `requester_name` (Optional[str]): 申请人用户名
  - `change_type` (str): 变更类型
  - `sql_text` (str): SQL 语句
  - `description` (Optional[str]): 描述
  - `status` (str): 状态
  - `approvals` (int): 已通过数
  - `required_approvals` (int): 所需通过数
  - `approved_by` (list[str]): 已审核人 ID 列表
  - `reject_reason` (Optional[str]): 驳回原因
  - `execute_message` (Optional[str]): 执行信息
  - `created_at` (str): 创建时间（ISO 字符串）
- **权限表达式**: `db_change:submit:write`

### 2. 获取 SQL 变更审核列表

- **方法**: GET
- **路径**: `/api/db-changes/`
- **描述**: 获取 SQL 变更审核列表（默认只看 pending 状态）
- **查询参数**:
  - `status` (str, 选填): 状态过滤；不传则只返回 pending
- **响应体** (`SqlChangeListResponse`):
  - `items` (list[`SqlChangeItem`]): 变更列表
  - `total` (int): 总数
- **权限表达式**: `sql_review:read`

### 3. 通过 SQL 变更审核

- **方法**: POST
- **路径**: `/api/db-changes/{change_id}/approve`
- **描述**: 审核通过 SQL 变更；达到 2 人通过后自动执行 SQL。不允许审核自己提交的请求，不允许重复审核
- **路径参数**:
  - `change_id` (str): 变更请求 ID
- **响应体** (`SqlChangeItem`)
- **权限表达式**: `db_change:approve:write&!isSelf(user_id)`

### 4. 驳回 SQL 变更审核

- **方法**: POST
- **路径**: `/api/db-changes/{change_id}/reject`
- **描述**: 驳回 SQL 变更请求，记录驳回原因，并向提交者发送通知
- **路径参数**:
  - `change_id` (str): 变更请求 ID
- **请求体**:
  - `reason` (str, 选填, 默认 ""): 驳回原因
- **响应体** (`SqlChangeItem`)
- **权限表达式**: `db_change:reject:write`

---

## 十四、用户管理模块 `/api/v2/users`

### 1. 获取用户列表

- **方法**: GET
- **路径**: `/api/v2/users`
- **描述**: 获取用户列表（非管理员/经理仅返回本人及同角色用户）
- **响应体**: list[`UserDetailResponse`]
  - `id` (str): 用户 ID
  - `username` (str): 用户名
  - `email` (Optional[str]): 邮箱
  - `nickname` (str): 昵称
  - `role` (str): 角色标识
  - `avatar_url` (Optional[str]): 头像 URL
  - `created_at` (datetime): 创建时间
  - `roles` (list[`RoleAssignmentInfo`]): 角色分配列表
    - `id` (str): 角色 ID
    - `name` (str): 角色标识
    - `display_name` (str): 显示名
    - `role_type` (str, 默认 "other"): 角色类型
- **权限表达式**: `users:read&(isAdmin()||isSelf()||hasSameRole(target_user))`

### 2. 创建用户

- **方法**: POST
- **路径**: `/api/v2/users`
- **描述**: 创建新用户；非管理员/经理提交将进入审核流程并返回 202
- **请求体** (`CreateUserRequest`):
  - `username` (str, 必填, 1-100): 用户名
  - `password` (str, 必填, 6-255): 密码
  - `nickname` (str, 必填, 1-100): 昵称
  - `email` (Optional[str], 选填, 最长 255): 邮箱
  - `avatar_url` (Optional[str], 选填): 头像 URL
  - `role` (Optional[str], 默认 "operator"): 角色标识（不可为 admin）
  - `role_ids` (list[str], 默认 []): RBAC 角色 ID 列表
- **响应体** (`UserDetailResponse`)；非管理员提交审核时返回 202 + 错误详情
- **权限表达式**: `users:create:write`

### 3. 获取用户详情

- **方法**: GET
- **路径**: `/api/v2/users/{user_id}`
- **描述**: 获取指定用户详情（非管理角色仅可访问同角色用户或本人）
- **路径参数**:
  - `user_id` (str): 用户 ID
- **响应体** (`UserDetailResponse`)
- **权限表达式**: `users:read&(isAdmin()||isSelf(user_id)||hasSameRole(target_user))`

### 4. 更新用户信息

- **方法**: PUT
- **路径**: `/api/v2/users/{user_id}`
- **描述**: 更新用户基本信息及角色分配（仅管理员可改 role_ids，超管角色不可修改）
- **路径参数**:
  - `user_id` (str): 用户 ID
- **请求体** (`UpdateUserRequest`):
  - `nickname` (Optional[str], 选填, 1-100): 昵称
  - `email` (Optional[str], 选填, 最长 255): 邮箱
  - `avatar_url` (Optional[str], 选填): 头像 URL
  - `role_ids` (Optional[list[str]], 选填): RBAC 角色 ID 列表
- **响应体** (`UserDetailResponse`)
- **权限表达式**: `users:update:write&(isAdmin()||isSelf(user_id)||hasSameRole(target_user))`

### 5. 更新用户角色分配

- **方法**: PUT
- **路径**: `/api/v2/users/{user_id}/roles`
- **描述**: 全量替换用户的 RBAC 角色分配（超管角色不可修改，仅管理员/经理可操作）
- **路径参数**:
  - `user_id` (str): 用户 ID
- **请求体** (`UpdateUserRolesRequest`):
  - `role_ids` (list[str], 必填): 角色 ID 列表
- **响应体** (`UserDetailResponse`)
- **权限表达式**: `users:update:write&isAdmin()`

### 6. 修改用户密码

- **方法**: PUT
- **路径**: `/api/v2/users/{user_id}/password`
- **描述**: 修改用户密码；非管理员重置非默认密码时需提供旧密码
- **路径参数**:
  - `user_id` (str): 用户 ID
- **请求体** (`UpdateUserPasswordRequest`):
  - `new_password` (str, 必填, 6-255): 新密码
  - `old_password` (Optional[str], 选填): 旧密码（非默认密码场景必填）
- **响应体**:
  - `message` (str): 提示信息
- **权限表达式**: `users:change_password:write&(isAdmin()||isSelf(user_id))`

### 7. 获取用户权限

- **方法**: GET
- **路径**: `/api/v2/users/{user_id}/permissions`
- **描述**: 获取用户生效权限与可用权限列表（非本人查看需 `users:custom_permissions:read`）
- **路径参数**:
  - `user_id` (str): 用户 ID
- **响应体** (`UserPermissionsResponse`):
  - `effective_permissions` (dict[str, str]): 生效权限 key → 资源名
  - `available_permissions` (list[`AvailablePermission`]): 可用权限列表
    - `id` (str): 权限 ID
    - `key` (str): 权限 key
    - `operation` (str): 操作名
    - `is_active` (bool): 是否启用
    - `created_at` (datetime): 创建时间
    - `resource` (`ResourceRef`): 关联资源
      - `id` (str): 资源 ID
      - `key` (str): 资源 key
      - `name` (str): 资源名
      - `description` (Optional[str]): 资源描述
- **权限表达式**: `users:custom_permissions:read||isSelf(user_id)`

### 8. 获取用户权限覆盖列表

- **方法**: GET
- **路径**: `/api/v2/users/{user_id}/permission-overrides`
- **描述**: 获取用户的自定义权限覆盖列表
- **路径参数**:
  - `user_id` (str): 用户 ID
- **响应体**: list[`PermissionOverrideEntry`]
  - `permission_key` (str): 权限 key
  - `granted` (bool): 是否授予
- **权限表达式**: `users:custom_permissions:read||isSelf(user_id)`

### 9. 更新用户权限覆盖

- **方法**: PUT
- **路径**: `/api/v2/users/{user_id}/permission-overrides`
- **描述**: 全量替换用户的自定义权限覆盖（超管账号不可修改）
- **路径参数**:
  - `user_id` (str): 用户 ID
- **请求体** (`UpdateUserPermissionOverridesRequest`):
  - `overrides` (list[`PermissionOverrideEntry`], 必填): 覆盖列表
    - `permission_key` (str): 权限 key
    - `granted` (bool): 是否授予
- **响应体**: list[`PermissionOverrideEntry`]
- **权限表达式**: `users:custom_permissions:write`

### 10. 重置用户权限覆盖

- **方法**: DELETE
- **路径**: `/api/v2/users/{user_id}/permission-overrides`
- **描述**: 移除用户全部自定义权限覆盖记录，恢复为角色默认权限（超管账号不可重置，非管理员仅可重置同角色用户）
- **路径参数**:
  - `user_id` (str): 用户 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `users:custom_permissions:write`

### 11. 删除用户

- **方法**: DELETE
- **路径**: `/api/v2/users/{user_id}`
- **描述**: 删除用户（超管不可删除，不可删除自己）
- **路径参数**:
  - `user_id` (str): 用户 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `users:delete:write&!isBuiltInAdmin(user_id)&!isSelf(user_id)&(isAdmin()||hasSameRole(target_user))`

### 12. 获取用户密码状态

- **方法**: GET
- **路径**: `/api/v2/users/{user_id}/password-status`
- **描述**: 检查用户是否使用默认密码（`{username}123`）
- **路径参数**:
  - `user_id` (str): 用户 ID
- **响应体**:
  - `is_default_password` (bool): 是否为默认密码
- **权限表达式**: `users:read`

---

## 十五、角色管理模块 `/api/v2/roles`

### 1. 获取角色列表

- **方法**: GET
- **路径**: `/api/v2/roles`
- **描述**: 获取所有角色列表（含父子关系及祖先/后代闭包）
- **响应体**: list[`RoleListItem`]
  - `id` (str): 角色 ID
  - `name` (str): 角色标识
  - `display_name` (str): 显示名
  - `description` (Optional[str]): 描述
  - `role_type` (str): 角色类型
  - `is_super_admin` (bool): 是否超管
  - `is_builtin` (bool): 是否内置
  - `created_at` (datetime): 创建时间
  - `updated_at` (datetime): 更新时间
  - `parent_roles` (list[`RoleRef`]): 直接父角色
  - `child_roles` (list[`RoleRef`]): 直接子角色
  - `all_ancestors` (list[`RoleRef`]): 所有祖先
  - `all_descendants` (list[`RoleRef`]): 所有后代
- **权限表达式**: `roles:read`

### 2. 创建角色

- **方法**: POST
- **路径**: `/api/v2/roles`
- **描述**: 创建新角色并设置父角色继承关系
- **请求体** (`CreateRoleRequest`):
  - `name` (str, 必填, 1-50): 角色标识
  - `display_name` (str, 必填, 1-100): 显示名
  - `description` (Optional[str], 选填): 描述
  - `role_type` (Literal["admin","other"], 默认 "other"): 角色类型
  - `parent_role_ids` (list[str], 默认 []): 父角色 ID 列表
- **响应体** (`RoleDetailResponse`)
- **权限表达式**: `roles:manage:write`

### 3. 获取角色详情

- **方法**: GET
- **路径**: `/api/v2/roles/{role_id}`
- **描述**: 获取指定角色详情
- **路径参数**:
  - `role_id` (str): 角色 ID
- **响应体** (`RoleDetailResponse`)
- **权限表达式**: `roles:read`

### 4. 更新角色

- **方法**: PUT
- **路径**: `/api/v2/roles/{role_id}`
- **描述**: 更新角色基本信息（内置角色不可改类型）
- **路径参数**:
  - `role_id` (str): 角色 ID
- **请求体** (`UpdateRoleRequest`):
  - `display_name` (Optional[str], 选填, 1-100): 显示名
  - `description` (Optional[str], 选填): 描述
  - `role_type` (Optional[Literal["admin","other"]], 选填): 角色类型
- **响应体** (`RoleDetailResponse`)
- **权限表达式**: `roles:manage:write`

### 5. 删除角色

- **方法**: DELETE
- **路径**: `/api/v2/roles/{role_id}`
- **描述**: 删除角色及其继承关系与权限关联（内置角色不可删除）
- **路径参数**:
  - `role_id` (str): 角色 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `roles:manage:write`

### 6. 添加父角色

- **方法**: POST
- **路径**: `/api/v2/roles/{role_id}/parents`
- **描述**: 为指定角色添加父角色（会校验循环继承）
- **路径参数**:
  - `role_id` (str): 子角色 ID
- **请求体** (`RoleRef`):
  - `id` (str, 必填): 父角色 ID
  - `name` (str): 名称（无需实际匹配）
  - `display_name` (str): 显示名
- **响应体** (`RoleDetailResponse`)
- **权限表达式**: `roles:manage:write`

### 7. 移除父角色

- **方法**: DELETE
- **路径**: `/api/v2/roles/{role_id}/parents/{parent_id}`
- **描述**: 移除指定角色的某个父角色继承关系
- **路径参数**:
  - `role_id` (str): 子角色 ID
  - `parent_id` (str): 父角色 ID
- **响应体** (`RoleDetailResponse`)
- **权限表达式**: `roles:manage:write`

### 8. 获取角色生效权限

- **方法**: GET
- **路径**: `/api/v2/roles/{role_id}/permissions`
- **描述**: 获取角色及其祖先的生效权限（key → 资源名）
- **路径参数**:
  - `role_id` (str): 角色 ID
- **响应体**: dict[str, str]（权限 key → 资源名）
- **权限表达式**: `isAuthenticated`

### 9. 获取角色直接权限

- **方法**: GET
- **路径**: `/api/v2/roles/{role_id}/permissions/direct`
- **描述**: 获取角色直接分配的权限列表（不含继承）
- **路径参数**:
  - `role_id` (str): 角色 ID
- **响应体**: list[`RolePermissionItem`]
  - `id` (str): 关联 ID
  - `key` (str): 权限 key
  - `operation` (str): 操作名
  - `resource_id` (str): 资源 ID
  - `resource_key` (str): 资源 key
  - `resource_name` (str): 资源名
  - `grant_type` (Literal["direct","inherited"]): 授予类型（此处恒为 direct）
- **权限表达式**: `roles:read`

### 10. 获取角色权限详情（含继承）

- **方法**: GET
- **路径**: `/api/v2/roles/{role_id}/permissions/detail`
- **描述**: 获取角色直接及继承的权限详情列表
- **路径参数**:
  - `role_id` (str): 角色 ID
- **响应体**: list[`RolePermissionItem`]（`grant_type` 标识 direct/inherited）
- **权限表达式**: `roles:read`

### 11. 更新角色权限

- **方法**: PUT
- **路径**: `/api/v2/roles/{role_id}/permissions`
- **描述**: 全量替换角色直接权限（超管角色不可修改）
- **路径参数**:
  - `role_id` (str): 角色 ID
- **请求体** (`UpdateRolePermissionsRequest`):
  - `permission_ids` (list[str], 必填): 权限 ID 列表
- **响应体**: list[`RolePermissionItem`]（返回直接权限列表）
- **权限表达式**: `roles:manage:write`

### 12. 预览继承权限

- **方法**: GET
- **路径**: `/api/v2/roles/{role_id}/permissions/preview/{parent_role_id}`
- **描述**: 预览将某父角色加入继承后，可继承到的权限（会校验循环继承）
- **路径参数**:
  - `role_id` (str): 当前角色 ID
  - `parent_role_id` (str): 待加入的父角色 ID
- **响应体**: dict[str, str]（权限 key → 资源名）
- **权限表达式**: `roles:read`

### 13. 获取角色继承关系

- **方法**: GET
- **路径**: `/api/v2/roles/{role_id}/inheritance`
- **描述**: 获取角色的祖先链与后代树（含每层直接权限与层级深度）
- **路径参数**:
  - `role_id` (str): 角色 ID
- **响应体** (`InheritanceResponse`):
  - `role` (`InheritanceRoleRef`): 当前角色
    - `id`、`name`、`display_name`、`role_type`、`is_super_admin`、`is_builtin`
  - `direct_permissions` (dict[str, str]): 当前角色直接权限
  - `ancestor_chain` (list[`InheritanceNode`]): 祖先链
    - `role` (`InheritanceRoleRef`): 角色
    - `direct_permissions` (dict[str, str]): 直接权限
    - `level` (int): 层级深度
  - `descendant_tree` (list[`InheritanceNode`]): 后代树（结构同上）
- **权限表达式**: `roles:read`

---

## 十六、权限管理模块 `/api/v2`

### 1. 获取当前用户权限

- **方法**: GET
- **路径**: `/api/v2/me/permissions`
- **描述**: 获取当前登录用户的生效权限（应用 deny override）
- **响应体**: dict[str, str]（权限 key → 资源名）
- **权限表达式**: `isAuthenticated`

### 2. 权限枚举列表

- **方法**: GET
- **路径**: `/api/v2/permission-enums`
- **描述**: 获取所有已启用权限的枚举（key + 资源名 + 描述）
- **响应体**: list[`PermissionEnumDetail`]
  - `key` (str): 权限 key
  - `name` (str): 资源名
  - `description` (Optional[str]): 资源描述
- **权限表达式**: `permissions:read||permissions:manage:read`

### 3. 页面权限列表

- **方法**: GET
- **路径**: `/api/v2/permission-pages`
- **描述**: 获取所有页面型权限（key 格式为 `{name}:read`）
- **响应体**: list[`PermissionEnumDetail`]
- **权限表达式**: `permissions:read||permissions:manage:read`

### 4. 获取资源树

- **方法**: GET
- **路径**: `/api/v2/resources`
- **描述**: 获取所有资源并以树形结构返回
- **响应体**: list[`ResourceNode`]
  - `id` (str): 资源 ID
  - `key` (str): 资源 key
  - `name` (str): 资源名
  - `description` (Optional[str]): 描述
  - `parent_id` (Optional[str]): 父资源 ID
  - `is_active` (bool): 是否启用
  - `created_at` (datetime): 创建时间
  - `children` (list[`ResourceNode`]): 子资源列表
- **权限表达式**: `permissions:read||permissions:manage:read`

### 5. 创建资源

- **方法**: POST
- **路径**: `/api/v2/resources`
- **描述**: 创建新资源（key 格式：page 为 `{name}:read`，action 为 `{name}:{operation}:{read|write}`）
- **请求体** (`CreateResourceRequest`):
  - `key` (str, 必填): 资源 key
  - `name` (str, 必填): 资源名
  - `description` (Optional[str], 选填): 描述
  - `parent_id` (Optional[str], 选填): 父资源 ID
  - `is_active` (bool, 默认 True): 是否启用
- **响应体** (`ResourceNode`)
- **权限表达式**: `permissions:manage:write`

### 6. 更新资源

- **方法**: PUT
- **路径**: `/api/v2/resources/{resource_id}`
- **描述**: 更新资源信息
- **路径参数**:
  - `resource_id` (str): 资源 ID
- **请求体** (`UpdateResourceRequest`):
  - `key` (Optional[str], 选填): 资源 key
  - `name` (Optional[str], 选填, 1-100): 资源名
  - `description` (Optional[str], 选填): 描述
  - `is_active` (Optional[bool], 选填): 是否启用
- **响应体** (`ResourceRef`): 含 `id`、`key`、`name`、`description`
- **权限表达式**: `permissions:manage:write`

### 7. 删除资源

- **方法**: DELETE
- **路径**: `/api/v2/resources/{resource_id}`
- **描述**: 删除资源及其下所有权限
- **路径参数**:
  - `resource_id` (str): 资源 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `permissions:manage:write`

### 8. 获取权限列表

- **方法**: GET
- **路径**: `/api/v2/permissions`
- **描述**: 获取所有权限列表（含资源信息）
- **响应体**: list[`PermissionItem`]
  - `id` (str): 权限 ID
  - `key` (str): 权限 key
  - `operation` (str): 操作名
  - `is_active` (bool): 是否启用
  - `created_at` (datetime): 创建时间
  - `resource` (`ResourceRef`): 关联资源（含 `id`、`key`、`name`、`description`）
- **权限表达式**: `permissions:read||permissions:manage:read`

### 9. 创建权限

- **方法**: POST
- **路径**: `/api/v2/permissions`
- **描述**: 创建新权限（key 未提供时自动按 `{resource.key}:{operation}` 生成）
- **请求体** (`CreatePermissionRequest`):
  - `resource_id` (str, 必填): 资源 ID
  - `operation` (str, 必填, 1-50): 操作名
  - `key` (Optional[str], 选填): 权限 key
  - `is_active` (bool, 默认 True): 是否启用
- **响应体** (`PermissionItem`)
- **权限表达式**: `permissions:manage:write`

### 10. 更新权限

- **方法**: PUT
- **路径**: `/api/v2/permissions/{permission_id}`
- **描述**: 更新权限信息
- **路径参数**:
  - `permission_id` (str): 权限 ID
- **请求体** (`UpdatePermissionRequest`):
  - `key` (Optional[str], 选填): 权限 key
  - `operation` (Optional[str], 选填): 操作名
  - `is_active` (Optional[bool], 选填): 是否启用
- **响应体** (`PermissionItem`)
- **权限表达式**: `permissions:manage:write`

### 11. 删除权限

- **方法**: DELETE
- **路径**: `/api/v2/permissions/{permission_id}`
- **描述**: 删除权限
- **路径参数**:
  - `permission_id` (str): 权限 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `permissions:manage:write`

---

## 十七、约束管理模块 `/api/v2/constraints`

### 1. 获取约束列表

- **方法**: GET
- **路径**: `/api/v2/constraints`
- **描述**: 获取所有角色约束（含角色关联）
- **响应体**: list[`ConstraintListItem`]
  - `id` (str): 约束 ID
  - `name` (str): 约束名
  - `description` (Optional[str]): 描述
  - `constraint_type` (str): 约束类型（mutual_exclusive / prerequisite / cardinality）
  - `config` (dict): 配置
  - `is_active` (bool): 是否启用
  - `created_at` (datetime): 创建时间
  - `roles` (list[`RoleAssociationInfo`]): 角色关联列表
    - `id` (str): 关联 ID
    - `role_id` (str): 角色 ID
    - `role_name` (str): 角色标识
    - `role_display_name` (str): 角色显示名
    - `association_type` (str): 关联类型（subject / prerequisite）
- **权限表达式**: `constraints:read`

### 2. 创建约束

- **方法**: POST
- **路径**: `/api/v2/constraints`
- **描述**: 创建新约束并设置角色关联
- **请求体** (`CreateConstraintRequest`):
  - `name` (str, 必填, 1-100): 约束名
  - `description` (Optional[str], 选填): 描述
  - `constraint_type` (Literal["mutual_exclusive","prerequisite","cardinality"], 必填): 约束类型
  - `config` (dict, 默认 {}): 配置
  - `is_active` (bool, 默认 True): 是否启用
  - `role_associations` (list[dict], 默认 []): 角色关联
    - `role_id` (str): 角色 ID
    - `association_type` (str): 关联类型（subject / prerequisite，受约束类型限制）
- **响应体** (`ConstraintDetailResponse`): 结构同 `ConstraintListItem`
- **权限表达式**: `constraints:manage:write`

### 3. 获取约束详情

- **方法**: GET
- **路径**: `/api/v2/constraints/{constraint_id}`
- **描述**: 获取指定约束详情
- **路径参数**:
  - `constraint_id` (str): 约束 ID
- **响应体** (`ConstraintDetailResponse`)
- **权限表达式**: `constraints:read`

### 4. 更新约束

- **方法**: PUT
- **路径**: `/api/v2/constraints/{constraint_id}`
- **描述**: 更新约束信息及角色关联（全量替换）
- **路径参数**:
  - `constraint_id` (str): 约束 ID
- **请求体** (`UpdateConstraintRequest`):
  - `name` (Optional[str], 选填, 1-100): 约束名
  - `description` (Optional[str], 选填): 描述
  - `config` (Optional[dict], 选填): 配置
  - `is_active` (Optional[bool], 选填): 是否启用
  - `role_associations` (Optional[list[dict]], 选填): 角色关联（结构同创建）
- **响应体** (`ConstraintDetailResponse`)
- **权限表达式**: `constraints:manage:write`

### 5. 删除约束

- **方法**: DELETE
- **路径**: `/api/v2/constraints/{constraint_id}`
- **描述**: 删除约束及其角色关联
- **路径参数**:
  - `constraint_id` (str): 约束 ID
- **响应体**: 无（HTTP 204）
- **权限表达式**: `constraints:manage:write`

---

## 十八、通知消息字典模块 `/api/notification-dict`

> 通知消息字段/枚举字典管理，用于通知内容模板化渲染。GET 接口仅校验登录态；写操作需 `notification:enum:write` 权限。

### 1. 获取字典分组列表

- **方法**: GET
- **路径**: `/api/notification-dict/groups`
- **描述**: 获取全部字典条目并按 `group_key` 分组返回
- **响应体**: list[`NotificationDictGroupResponse`]
  - `group_key` (str): 分组 key
  - `items` (list[`NotificationDictResponse`]): 该分组下条目列表
    - `id` (str): 条目 ID
    - `category` (str): 类别（`field` / `enum`）
    - `group_key` (str): 分组 key
    - `dict_key` (str): 字典 key
    - `dict_value` (str): 字典值
    - `is_active` (bool): 是否启用
    - `is_private` (bool): 是否隐私化（隐藏原始值）
    - `sort_order` (int): 排序权重
    - `created_at` (datetime): 创建时间
    - `updated_at` (datetime): 更新时间
- **权限表达式**: `isAuthenticated`

### 2. 获取字典列表

- **方法**: GET
- **路径**: `/api/notification-dict/`
- **描述**: 获取字典条目列表，可按类别和分组过滤
- **查询参数**:
  - `category` (str, 选填): 类别过滤（`field` / `enum`）
  - `group_key` (str, 选填): 分组过滤
- **响应体** (`NotificationDictListResponse`):
  - `items` (list[`NotificationDictResponse`]): 条目列表（结构同分组接口）
  - `total` (int): 总数
- **权限表达式**: `isAuthenticated`

### 3. 创建字典条目

- **方法**: POST
- **路径**: `/api/notification-dict/`
- **描述**: 创建新的字典条目（同分组下 `dict_key` 唯一）
- **请求体** (`NotificationDictCreate`):
  - `category` (str, 必填): 类别（`field` / `enum`）
  - `group_key` (str, 必填, 最长 100): 分组 key
  - `dict_key` (str, 必填, 最长 200): 字典 key
  - `dict_value` (str, 必填, 最长 200): 字典值
  - `is_active` (bool, 默认 True): 是否启用
  - `is_private` (bool, 默认 False): 是否隐私化处理
  - `sort_order` (int, 默认 0): 排序权重
- **响应体** (`NotificationDictResponse`)
- **权限表达式**: `notification:enum:write`

### 4. 更新字典条目

- **方法**: PUT
- **路径**: `/api/notification-dict/{entry_id}`
- **描述**: 更新指定字典条目
- **路径参数**:
  - `entry_id` (str): 条目 ID
- **请求体** (`NotificationDictUpdate`, 均选填):
  - `category` (Optional[str]): 类别
  - `group_key` (Optional[str]): 分组 key
  - `dict_key` (Optional[str]): 字典 key
  - `dict_value` (Optional[str]): 字典值
  - `is_active` (Optional[bool]): 是否启用
  - `is_private` (Optional[bool]): 是否隐私化
  - `sort_order` (Optional[int]): 排序权重
- **响应体** (`NotificationDictResponse`)
- **权限表达式**: `notification:enum:write`

### 5. 删除字典条目

- **方法**: DELETE
- **路径**: `/api/notification-dict/{entry_id}`
- **描述**: 删除指定字典条目
- **路径参数**:
  - `entry_id` (str): 条目 ID
- **响应体** (`MessageResponse`):
  - `message` (str): 提示信息
- **权限表达式**: `notification:enum:write`
- **说明**: 该接口返回 200 + `MessageResponse`，**未遵循 RESTful 的 204 无内容规范**，属历史遗留

---

## RESTful API 设计规范

### 1. HTTP 方法语义

| 方法   | 语义                       | 项目使用情况                                                           |
| ------ | -------------------------- | ---------------------------------------------------------------------- |
| GET    | 查询资源（幂等、无副作用） | 全部查询接口，含 SSE 流式接口                                          |
| POST   | 创建资源 / 触发动作        | 资源创建返回 201；动作型接口（如 retry、approve、ai-generate）返回 200 |
| PUT    | 全量更新资源               | 部分更新场景使用 `Optional` 字段 + PUT 实现，**未使用 PATCH**          |
| DELETE | 删除资源                   | 资源删除返回 204；少数返回 200 + body（见下方说明）                    |
| PATCH  | 部分更新                   | **项目未使用**                                                         |

### 2. 状态码规范

| 场景            | 状态码  | 说明                                                                                                                        |
| --------------- | ------- | --------------------------------------------------------------------------------------------------------------------------- |
| GET 查询成功    | 200     | 默认                                                                                                                        |
| POST 创建资源   | **201** | 资源型 POST 统一返回 201（accounts/contents/templates/publish/users/roles/resources/permissions/constraints/model-configs） |
| POST 动作触发   | 200     | 动作型 endpoint（retry/approve/reject/submit/check/ai-generate/read 等）返回 200                                            |
| PUT 更新成功    | 200     | 返回更新后的资源                                                                                                            |
| DELETE 删除资源 | **204** | 无响应体                                                                                                                    |
| DELETE 动作型   | 200     | 需返回更新后资源或提示信息时用 200（见下方例外）                                                                            |
| 认证失败        | 401     | JWT 缺失/失效                                                                                                               |
| 权限不足        | 403     | 权限表达式求值为 false                                                                                                      |
| 资源不存在      | 404     | 路径参数指向的资源不存在                                                                                                    |
| 参数错误        | 400     | 请求体校验失败 / 业务规则冲突                                                                                               |
| 资源冲突        | 409     | 唯一约束冲突（如 dict_key 重复）                                                                                            |
| 异步接受        | 202     | 非管理员创建用户进入审核流程                                                                                                |

#### DELETE 返回 200 的例外（业务需返回数据）

| 接口                                                 | 状态码 | 原因                                         |
| ---------------------------------------------------- | ------ | -------------------------------------------- |
| DELETE `/api/v2/roles/{role_id}/parents/{parent_id}` | 200    | 返回更新后的角色详情（`RoleDetailResponse`） |
| DELETE `/api/notification-dict/{entry_id}`           | 200    | 返回 `MessageResponse` 提示信息（历史遗留）  |

### 3. 路径设计规范

#### 3.1 路径末尾斜杠

项目当前存在两种风格（FastAPI 默认不重定向，需精确匹配）：

| 风格                     | 示例                                                                                                                                                                  | 出现模块 |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| 集合路径**带**末尾斜杠   | `/api/accounts/`、`/api/contents/`、`/api/templates/`、`/api/notifications/`、`/api/reviews/`、`/api/db-changes/`、`/api/notification-dict/`                          | 早期模块 |
| 集合路径**不带**末尾斜杠 | `/api/v2/users`、`/api/v2/roles`、`/api/v2/permissions`、`/api/v2/constraints`、`/api/model-configs`、`/api/publish/tasks`、`/api/dashboard/stats`、`/api/auth/login` | 新模块   |

> **新增接口规范**：统一**不带**末尾斜杠，与 RBAC v2 模块保持一致。

#### 3.2 资源嵌套路径

```
/{resource}                      # 集合
/{resource}/{id}                 # 详情
/{resource}/{id}/{sub-resource}  # 子资源
/{resource}/{id}/{action}        # 动作（POST）
```

#### 3.3 路由声明顺序

当静态路径与动态路径参数同前缀时，**静态路径必须先声明**：

```
GET /api/contents/ai-generations    # 静态，先声明
GET /api/contents/{content_id}      # 动态，后声明
```

```
GET /api/notifications/stream       # 静态，先声明
GET /api/notifications/             # 动态集合，后声明
```

### 4. 响应格式规范

#### 4.1 单资源

直接返回资源对象（Pydantic Model）：

```json
{ "id": "...", "name": "...", ... }
```

#### 4.2 列表（无分页）

直接返回数组：

```json
[{ "id": "..." }, { "id": "..." }]
```

#### 4.3 列表（分页）

返回包含 `total` / `page` / `page_size` / `items` 的对象：

```json
{ "total": 100, "page": 1, "page_size": 20, "items": [ ... ] }
```

> 部分早期接口（如 `/api/contents/`、`/api/accounts/`）直接返回数组无分页元数据，属历史遗留。

#### 4.4 动作型响应

返回 `{"message": "..."}` 提示信息：

```json
{ "message": "密码修改成功" }
```

### 5. 权限表达式与实际校验的关系

项目权限校验存在**两层机制**，文档中"权限表达式"列的含义需注意区分：

#### 5.1 装饰器层（`@RequiresPermissions`）

- 在请求进入函数体前由 `PermAPIRoute` 中间件求值
- 仅校验**基础权限 key**（如 `users:read`、`roles:manage:write`）
- 当前后端代码中，装饰器**主要使用单一权限 key**，不使用复杂表达式 DSL

#### 5.2 函数内手工校验

部分接口的复杂权限逻辑（如 `isAdmin()||isSelf()||hasSameRole()`）在**函数体内**通过 `has_permission_direct` 手工判断：

| 接口                                          | 装饰器权限           | 函数内手工校验                                                               |
| --------------------------------------------- | -------------------- | ---------------------------------------------------------------------------- |
| GET `/api/v2/users`                           | `users:read`         | `isAdmin() \|\| isSelf() \|\| hasSameRole()` 过滤可见范围                    |
| GET `/api/v2/users/{id}`                      | `users:read`         | `isAdmin() \|\| isSelf(user_id) \|\| hasSameRole(target_user)`               |
| PUT `/api/v2/users/{id}`                      | `users:update:write` | 同上                                                                         |
| DELETE `/api/v2/users/{id}`                   | `users:delete:write` | `!isBuiltInAdmin(user_id) & !isSelf(user_id) & (isAdmin()\|\|hasSameRole())` |
| GET `/api/v2/users/{id}/permissions`          | 无装饰器             | `isSelf(user_id) \|\| users:custom_permissions:read`                         |
| GET `/api/v2/users/{id}/permission-overrides` | 无装饰器             | 同上                                                                         |
| GET `/api/v2/roles/{id}/permissions`          | 无装饰器             | 仅校验登录态                                                                 |

> **文档约定**：本文档"权限表达式"列记录的是**业务上应满足的完整权限逻辑**（含函数内手工校验），而非仅装饰器声明。前端路由守卫和 `v-perm` 指令使用此完整表达式进行前端侧预校验。

#### 5.3 无装饰器但需登录的接口

以下接口未挂载 `@RequiresPermissions`，仅通过 `get_current_user` 依赖校验 JWT：

- `auth.py` 全部接口（认证模块本身）
- `notifications.py` 全部接口（用户自有通知）
- GET `/api/v2/me/permissions`（查自己权限）
- GET `/api/v2/roles/{role_id}/permissions`（查角色权限，仅校验登录态）
- GET `/api/v2/users/{user_id}/permissions`（函数内手工校验）
- GET `/api/v2/users/{user_id}/permission-overrides`（函数内手工校验）
- GET `/api/notification-dict/groups`、GET `/api/notification-dict/`（字典查询仅校验登录态）

### 6. 权限 key 命名规范

#### 6.1 推荐格式（三段式）

```
{resource}:{operation}:{read|write}
```

- `resource`：资源名，复数（如 `users`、`roles`、`contents`）
- `operation`：操作名（`create` / `update` / `delete` / `read` / `custom_permissions` 等）
- `read|write`：读写区分

示例：`users:create:write`、`content:update:write`、`accounts:read`

#### 6.2 页面型权限（两段式）

```
{resource}:read
```

用于页面访问控制，如 `dashboard:read`、`content:read`、`review:read`。

#### 6.3 现存命名不一致（历史遗留）

| 不一致类型             | 例子                                                                         | 说明                                                       |
| ---------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------- |
| 单复数混用             | `account:create:write`（单数） vs `accounts:read`（复数）                    | accounts 模块内不一致                                      |
| `manage` 统管增删改    | `roles:manage:write`、`constraints:manage:write`、`permissions:manage:write` | 一个权限覆盖增删改，与 `create/update/delete` 细分风格不同 |
| 四段式                 | `db_history:view:read`、`notification:enum:write`                            | 子资源命名                                                 |
| 跨业务复用             | `review:read`/`review:approve:write` 同时用于内容审核和用户创建审核          | 两类业务权限无法区分                                       |
| GET 与写操作命名不同源 | `token_plan:read`（GET） vs `model_config:create:write`（POST）              | model_configs 模块读用 `token_plan`，写用 `model_config`   |
| GET 与写操作命名不同源 | `sql_review:read`（GET） vs `db_change:submit:write`（POST）                 | db_changes 模块读用 `sql_review`，写用 `db_change`         |

> **新增权限 key 规范**：统一使用三段式 `{resource}:{operation}:{read|write}`，资源名用复数，避免 `manage` 统管和四段式。

---

## 权限判断函数实现说明

> 完整函数定义、参数规范及使用示例见 [权限表达式内置判断函数](权限表达式内置判断函数.md)。

### 前端实现（`frontend/src/utils/permExpression.ts`）

| 函数              | 参数形式                                 | 实现逻辑                                               |
| ----------------- | ---------------------------------------- | ------------------------------------------------------ |
| `isAuthenticated` | 无参                                     | 路由守卫已校验，表达式层恒返回 `true`                  |
| `isSuperAdmin`    | 无参                                     | `currentUser.role == "admin" && currentUser.id == "1"` |
| `isAdmin`         | 无参                                     | `currentUser.role in ["admin", "manager"]`             |
| `isSelf`          | 无参(`ctx.user_id`)/有参(`key_name`)     | `currentUser.id === ctx[key_name]`                     |
| `isBuiltInAdmin`  | 无参(`ctx.user_id`)/有参(`key_name`)     | `ctx[key_name] === "1"`                                |
| `isOwnContent`    | 无参(`ctx.content`)/有参(`key_name`)     | `ctx[key_name].user_id === currentUser.id`             |
| `isOwnAccount`    | 无参(`ctx.account`)/有参(`key_name`)     | `ctx[key_name].user_id === currentUser.id`             |
| `hasSameRole`     | 无参(`ctx.target_user`)/有参(`key_name`) | `current_role_ids ∩ target_role_ids ≠ ∅`               |
| `isAdminType`     | `isAdminType(role_type)`                 | 前端暂未实现，由后端约束校验使用                       |
| `isOtherType`     | `isOtherType(role_type)`                 | 前端暂未实现，由后端约束校验使用                       |

### 后端实现（`backend/app/services/perm_expression.py`）

| 函数              | 参数形式                                    | 实现逻辑                                                        |
| ----------------- | ------------------------------------------- | --------------------------------------------------------------- |
| `isAuthenticated` | 无参                                        | `current_user` 存在即 `true`（依赖层已校验）                    |
| `isSuperAdmin`    | 无参                                        | `current_user.role == "admin" and current_user.id == "1"`       |
| `isAdmin`         | 无参                                        | `current_user.role in {"admin", "manager"}`                     |
| `isSelf`          | 无参(`ctx["user_id"]`)/有参(`key_name`)     | `current_user.id == ctx[key_name]`                              |
| `isBuiltInAdmin`  | 无参(`ctx["user_id"]`)/有参(`key_name`)     | `ctx[key_name] == "1"`                                          |
| `isOwnContent`    | 无参(`ctx["content"]`)/有参(`key_name`)     | `ctx[key_name].user_id == current_user.id`                      |
| `isOwnAccount`    | 无参(`ctx["account"]`)/有参(`key_name`)     | `ctx[key_name].user_id == current_user.id`                      |
| `hasSameRole`     | 无参(`ctx["target_user"]`)/有参(`key_name`) | `current_role_ids & target_role_ids ≠ ∅`（后备 `all_role_ids`） |
| `isAdminType`     | `isAdminType(role_type)`                    | 由 `rbac_constraint_service` 使用，未注册到 `BUILTIN_FUNCTIONS` |
| `isOtherType`     | `isOtherType(role_type)`                    | 由 `rbac_constraint_service` 使用，未注册到 `BUILTIN_FUNCTIONS` |

## 接口个数统计

| 模块                           | 接口数量 | 公开接口 | 需鉴权接口 |
| ------------------------------ | -------- | -------- | ---------- |
| 认证 auth                      | 5        | 2        | 3          |
| 仪表盘 dashboard               | 1        | 0        | 1          |
| 平台账号 accounts              | 6        | 0        | 6          |
| 内容管理 contents              | 8        | 0        | 8          |
| 发布管理 publish               | 5        | 0        | 5          |
| 模板管理 templates             | 5        | 0        | 5          |
| 审核管理 reviews               | 4        | 0        | 4          |
| 用户注册审核                   | 3        | 0        | 3          |
| 模型配置 model-configs         | 4        | 0        | 4          |
| AI模型 models                  | 1        | 0        | 1          |
| 通知 notifications             | 6        | 0        | 6          |
| 通知消息字典 notification-dict | 5        | 0        | 5          |
| 数据库 db                      | 2        | 0        | 2          |
| SQL变更 db-changes             | 4        | 0        | 4          |
| 用户管理 v2/users              | 12       | 0        | 12         |
| 角色管理 v2/roles              | 13       | 0        | 13         |
| 权限管理 v2/permissions        | 11       | 0        | 11         |
| 约束管理 v2/constraints        | 5        | 0        | 5          |
| **合计**                       | **100**  | **2**    | **98**     |
